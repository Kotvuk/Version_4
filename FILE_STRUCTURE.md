# Структура файлов KotvukAI

## 📁 Организация кода

```
crypto-analyzer/
├── 📄 server.js                 # Express сервер
├── 📄 database.js               # Работа с SQLite БД
├── 📄 package.json              # Зависимости Node.js
├── 💾 kotvukai.db               # База данных SQLite
│
├── 📂 public/                   # Фронтенд приложения
│   │
│   ├── 🔐 АВТОРИЗАЦИЯ
│   ├── index.html               # Страница входа/регистрации
│   ├── styles.css               # Стили авторизации
│   └── auth.js                  # Логика входа/регистрации
│   │
│   ├── 🏠 ГЛАВНОЕ ПРИЛОЖЕНИЕ
│   ├── dashboard.html           # Основной HTML (структура)
│   ├── dashboard.css            # Все стили приложения
│   └── dashboard.js             # Главный файл (навигация, auth)
│   │
│   └── 📂 pages/                # МОДУЛИ СТРАНИЦ
│       ├── common.js            # Общие функции (страница "В разработке")
│       ├── dashboard-page.js   # ПАНЕЛЬ: Дашборд (виджеты + график)
│       ├── trades-page.js      # ПАНЕЛЬ: Список сделок (демо-счета)
│       ├── chart-page.js       # ПАНЕЛЬ: График (будет добавлено)
│       ├── ai-page.js          # ПАНЕЛЬ: ИИ Анализ (будет добавлено)
│       └── settings-page.js    # ПАНЕЛЬ: Настройки (будет добавлено)
│
├── 📂 Документация
├── DATABASE.md                  # Описание структуры БД
├── PROJECT_STATUS.md            # Статус проекта
└── FILE_STRUCTURE.md            # Этот файл
```

---

## 🎯 Где что находится

### **Если нужно исправить:**

#### ✅ **Авторизация**
- **Форма входа/регистрации:** `public/index.html`
- **Стили формы:** `public/styles.css`
- **Логика входа:** `public/auth.js`
- **Backend API:** `server.js` (строки с `/api/auth/`)

---

#### 📊 **Панель "Дашборд"**
- **HTML шаблон:** функция `getDashboardContent()` в `public/pages/dashboard-page.js`
- **Виджеты (баланс, профит, сделки, ИИ):** `loadDashboardStats()` в `dashboard-page.js`
- **График профита:** `loadProfitChart()` и `renderProfitChart()` в `dashboard-page.js`
- **Стили:** раздел "График профита" в `public/dashboard.css`
- **Backend API:** `server.js` (`/api/dashboard/stats`, `/api/dashboard/profit-chart`)
- **База данных:** функции `getDashboardStats()` и `getProfitChartData()` в `database.js`

---

#### 💰 **Панель "Список сделок"**
- **HTML шаблон:** функция `getTradesContent()` в `public/pages/trades-page.js`
- **Логика загрузки:** функции `loadTradesPage()`, `loadAccountData()` в `trades-page.js`
- **Переключатель счетов:** `renderAccountSwitcher()` в `trades-page.js`
- **Таблица сделок:** `renderTradesTable()` в `trades-page.js`
- **Стили:** раздел "Страница Список сделок" в `public/dashboard.css`
- **Backend API:** `server.js` (`/api/demo-accounts/*`)
- **База данных:** функции `getDemoAccounts()`, `getDemoAccountStats()` в `database.js`

---

#### 📈 **Панель "График"** (будет)
- **Файл:** `public/pages/chart-page.js` (создать)
- **Подключить в:** `public/dashboard.html` (добавить `<script>`)
- **Добавить функцию:** `getChartContent()` и `loadChartPage()`

---

#### 🤖 **Панель "ИИ Анализ"** (будет)
- **Файл:** `public/pages/ai-page.js` (создать)
- **Подключить в:** `public/dashboard.html`
- **Добавить функцию:** `getAIAnalysisContent()` и `loadAIAnalysisPage()`

---

#### ⚙️ **Панель "Настройки"** (будет)
- **Файл:** `public/pages/settings-page.js` (создать)
- **Подключить в:** `public/dashboard.html`
- **Добавить функцию:** `getSettingsContent()` и `loadSettingsPage()`

---

#### 🎨 **Стили**
- **Все стили:** `public/dashboard.css`
- **Разделы:**
  - Sidebar (левая панель)
  - Topbar (верхняя панель)
  - Дашборд (виджеты + график)
  - Список сделок (счета + таблица)
  - Страница "В разработке"

---

#### 🗄️ **База данных**
- **Файл БД:** `kotvukai.db` (SQLite)
- **Логика работы с БД:** `database.js`
- **Таблицы:**
  - `users` — пользователи
  - `demo_accounts` — демо-счета
  - `charts` — графики
  - `analyses` — ИИ анализы
  - `trades` — сделки
- **Миграции/изменения структуры:** редактировать `database.js`, удалить `kotvukai.db`, перезапустить

---

#### 🔌 **API Endpoints**
- **Файл:** `server.js`
- **Авторизация:**
  - `POST /api/auth/login`
  - `POST /api/auth/register`
- **Дашборд:**
  - `GET /api/dashboard/stats`
  - `GET /api/dashboard/profit-chart`
- **Демо-счета:**
  - `GET /api/demo-accounts`
  - `GET /api/demo-accounts/:id/stats`
  - `GET /api/demo-accounts/:id/trades`

---

## 🔧 Как добавить новую панель

1. **Создать файл:** `public/pages/new-page.js`
2. **Написать функции:**
   ```javascript
   function getNewPageContent() {
     return `<div>...</div>`;
   }
   
   function loadNewPage() {
     // логика загрузки данных
   }
   ```
3. **Подключить в HTML:** добавить `<script src="pages/new-page.js"></script>` в `dashboard.html`
4. **Добавить в навигацию:** в `dashboard.js` в функцию `loadPage()` добавить case для новой страницы
5. **Добавить стили:** в `dashboard.css` добавить секцию для новой страницы

---

## 📝 Примеры изменений

### **Изменить цвет виджета**
- Файл: `public/dashboard.css`
- Найти: `.stat-icon.green { background: ... }`
- Изменить цвет

### **Добавить виджет на дашборд**
- Файл: `public/pages/dashboard-page.js`
- Функция: `getDashboardContent()`
- Добавить HTML для нового виджета

### **Изменить логику загрузки сделок**
- Файл: `public/pages/trades-page.js`
- Функция: `loadAccountData(accountId)`

### **Добавить новое API**
- Файл: `server.js`
- Добавить новый endpoint после существующих

### **Изменить структуру БД**
- Файл: `database.js`
- Изменить SQL запрос создания таблицы
- Удалить `kotvukai.db`
- Перезапустить сервер (`node server.js`)

---

**Автор:** 祐志 牧谷  
**Проект:** KotvukAI  
**Дата:** 22.02.2026
