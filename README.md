# KotvukAI

**Кроссплатформенное приложение для автоматического анализа криптовалютных графиков с ИИ**

---

## 🚀 Быстрый старт

```bash
cd crypto-analyzer
npm install
node server.js
```

Приложение доступно на: **http://localhost:3000**

**Администратор по умолчанию:**
- Email: `admin@corp.kz`
- Пароль: `AdminDamir`

---

## 📋 Статус проекта

✅ **Реализовано:**
- Система авторизации (вход/регистрация)
- База данных SQLite
- Дашборд с виджетами и графиком профита
- Список сделок с демо-счетами (Пользовательский + ИИ)

❌ **В разработке:**
- График (просмотр криптовалютных графиков)
- ИИ Анализ (автоматический анализ графиков)
- Настройки

📚 **Подробнее:** [PROJECT_STATUS.md](PROJECT_STATUS.md)

---

## 📁 Структура кода

```
crypto-analyzer/
├── server.js              # Express сервер + API
├── database.js            # Логика работы с SQLite
├── kotvukai.db            # База данных
│
├── public/
│   ├── index.html         # Авторизация
│   ├── dashboard.html     # Главное приложение
│   ├── dashboard.js       # Навигация + общая логика
│   ├── dashboard.css      # Все стили
│   │
│   └── pages/             # МОДУЛИ СТРАНИЦ
│       ├── common.js           # Общие функции
│       ├── dashboard-page.js   # Панель: Дашборд
│       └── trades-page.js      # Панель: Список сделок
```

### 🎯 **Где что редактировать?**

| Что нужно изменить | Файл |
|--------------------|------|
| **Форма входа/регистрации** | `public/index.html`, `public/auth.js` |
| **Виджеты дашборда** | `public/pages/dashboard-page.js` |
| **График профита** | `public/pages/dashboard-page.js` → `loadProfitChart()` |
| **Список сделок** | `public/pages/trades-page.js` |
| **Стили** | `public/dashboard.css` |
| **API endpoints** | `server.js` |
| **База данных** | `database.js` |

📖 **Подробная документация:** [FILE_STRUCTURE.md](FILE_STRUCTURE.md)

---

## 🗄️ База данных

**Таблицы:**
- `users` — пользователи
- `demo_accounts` — демо-счета (пользовательский + ИИ)
- `charts` — графики
- `analyses` — ИИ анализы
- `trades` — сделки

**Документация БД:** [DATABASE.md](DATABASE.md)

---

## 🔌 API Endpoints

### Авторизация
- `POST /api/auth/login` — вход
- `POST /api/auth/register` — регистрация

### Дашборд
- `GET /api/dashboard/stats?userId=X` — статистика виджетов
- `GET /api/dashboard/profit-chart?userId=X&days=N` — данные графика

### Демо-счета
- `GET /api/demo-accounts?userId=X` — список счетов
- `GET /api/demo-accounts/:id/stats` — статистика счёта
- `GET /api/demo-accounts/:id/trades` — сделки счёта

---

## 🛠️ Технологии

**Backend:**
- Node.js + Express
- SQLite3 (база данных)
- bcryptjs (хеширование паролей)

**Frontend:**
- Vanilla JavaScript (без фреймворков)
- Chart.js (графики)
- Модульная архитектура (разделение по страницам)

**Дизайн:**
- Space Grotesk + Inter (шрифты)
- Тёмная тема
- Адаптивный дизайн

---

## 📝 TODO

- [ ] Панель "График" — добавление/просмотр криптовалютных графиков
- [ ] Панель "ИИ Анализ" — автоматический анализ с помощью ИИ
- [ ] Панель "Настройки" — настройки профиля и системы
- [ ] Интеграция с Binance API
- [ ] Реализация ИИ модели для анализа
- [ ] Desktop версия (Electron)
- [ ] Windows сборка

---

**Автор:** 祐志 牧谷  
**Тема дипломной работы:** Разработка кроссплатформенного (Windows и веб-приложение) с ИИ и системой авторизаций для автоматического анализа криптовалютных графиков  
**Дата:** 22.02.2026
