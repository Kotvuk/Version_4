# Демо-счета KotvukAI

## Концепция

Каждый пользователь получает **два демо-счёта**:

1. **👤 Пользовательский счёт** — для ручной торговли
2. **🤖 ИИ счёт** — для автоматической торговли ИИ

---

## Характеристики

- **Начальный баланс:** $10,000 на каждом счёте
- **Автоматическое создание:** при первом входе пользователя
- **Независимые балансы:** сделки на одном счёте не влияют на другой
- **Изолированная статистика:** у каждого счёта своя история сделок

---

## Зачем два счёта?

### Пользовательский счёт 👤
- Для **ручной торговли**
- Пользователь сам открывает/закрывает сделки
- Обучение трейдингу
- Тестирование стратегий

### ИИ счёт 🤖
- Для **автоматической торговли**
- ИИ открывает сделки на основе своих прогнозов
- ИИ видит результаты и учится на ошибках
- Сравнение эффективности: человек vs ИИ

---

## Как это работает

### 1. Создание счетов
При первом входе пользователя автоматически создаются оба счёта:

```javascript
// database.js
function createDemoAccounts(userId, callback) {
  // Создание пользовательского счёта
  db.run(
    'INSERT INTO demo_accounts (user_id, name, account_type, balance, initial_balance) VALUES (?, ?, ?, ?, ?)',
    [userId, 'Мой демо-счёт', 'user', 10000, 10000],
    ...
  );
  
  // Создание ИИ счёта
  db.run(
    'INSERT INTO demo_accounts (user_id, name, account_type, balance, initial_balance) VALUES (?, ?, ?, ?, ?)',
    [userId, 'ИИ демо-счёт', 'ai', 10000, 10000],
    ...
  );
}
```

### 2. Привязка сделок
Каждая сделка привязана к конкретному счёту:

```sql
CREATE TABLE trades (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  demo_account_id INTEGER NOT NULL,  -- ссылка на счёт
  user_id INTEGER NOT NULL,
  symbol TEXT NOT NULL,
  type TEXT NOT NULL,
  ...
  FOREIGN KEY (demo_account_id) REFERENCES demo_accounts(id)
)
```

### 3. Обновление баланса
При закрытии сделки баланс счёта обновляется:

```javascript
// Когда сделка закрывается
const profit = (exit_price - entry_price) * amount;

// Обновление баланса счёта
UPDATE demo_accounts 
SET balance = balance + profit 
WHERE id = demo_account_id;
```

---

## API Endpoints

### Получить счета пользователя
```
GET /api/demo-accounts?userId=1
```

**Ответ:**
```json
{
  "success": true,
  "accounts": [
    {
      "id": 1,
      "user_id": 1,
      "name": "Мой демо-счёт",
      "account_type": "user",
      "balance": 10000,
      "initial_balance": 10000,
      "created_at": "2026-02-22 14:32:32"
    },
    {
      "id": 2,
      "user_id": 1,
      "name": "ИИ демо-счёт",
      "account_type": "ai",
      "balance": 10000,
      "initial_balance": 10000,
      "created_at": "2026-02-22 14:32:32"
    }
  ]
}
```

### Получить статистику счёта
```
GET /api/demo-accounts/1/stats
```

**Ответ:**
```json
{
  "success": true,
  "stats": {
    "account": {
      "id": 1,
      "name": "Мой демо-счёт",
      "account_type": "user",
      "balance": 10000,
      "initial_balance": 10000
    },
    "totalTrades": 0,
    "activeTrades": 0,
    "totalProfit": 0
  }
}
```

### Получить сделки счёта
```
GET /api/demo-accounts/1/trades
```

**Ответ:**
```json
{
  "success": true,
  "trades": []
}
```

---

## Интерфейс

### Переключатель счетов
На странице "Список сделок" пользователь видит кнопки:

```
┌─────────────────────────────┐  ┌─────────────────────────────┐
│ 👤  Мой демо-счёт           │  │ 🤖  ИИ демо-счёт            │
│     Пользовательский        │  │     ИИ                      │
└─────────────────────────────┘  └─────────────────────────────┘
```

### Статистика счёта
При выборе счёта отображается:

- **Текущий баланс:** $10,000.00
- **Начальный баланс:** $10,000.00
- **Профит/убыток:** +$0.00 (+0.00%)
- **Всего сделок:** 0
- **Активных сделок:** 0

### Таблица сделок
Пустая таблица с сообщением "Нет сделок"

---

## База данных

### Таблица demo_accounts

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER | PRIMARY KEY |
| user_id | INTEGER | Владелец счёта |
| name | TEXT | Название счёта |
| account_type | TEXT | Тип: `user` или `ai` |
| balance | REAL | Текущий баланс |
| initial_balance | REAL | Начальный баланс |
| created_at | DATETIME | Дата создания |

---

## Будущие улучшения

- [ ] Возможность сброса счёта (вернуть к $10,000)
- [ ] История изменения баланса
- [ ] Статистика сравнения: пользователь vs ИИ
- [ ] Экспорт истории сделок
- [ ] Настройка начального баланса
- [ ] Создание дополнительных счетов

---

**Дата:** 22.02.2026  
**Проект:** KotvukAI
