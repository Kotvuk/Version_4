# KotvukAI - Статус проекта

**Дата:** 22.02.2026  
**Тема дипломной работы:** Разработка кроссплатформенного (Windows и веб-приложение) с ИИ и системой авторизаций для автоматического анализа криптовалютных графиков

---

## ✅ Реализовано

### 1. Система авторизации
- ✅ Форма входа
- ✅ Форма регистрации
- ✅ SVG иконки показа/скрытия пароля
- ✅ Валидация (минимум 6 символов)
- ✅ Хеширование паролей (bcrypt)
- ✅ SQLite база данных
- ✅ Администратор создаётся из переменных окружения (ADMIN_EMAIL/ADMIN_PASSWORD/ADMIN_NAME)

### 2. База данных (SQLite)
**Таблицы:**
- `users` - пользователи (id, name, email, password, balance, created_at)
- `charts` - графики (id, user_id, symbol, timeframe, is_active, created_at)
- `analyses` - ИИ анализы (id, chart_id, user_id, prediction, confidence, is_correct, created_at)
- `trades` - сделки (id, user_id, symbol, type, entry_price, exit_price, profit, status, created_at, closed_at)

### 3. Основное окно приложения
- ✅ Выдвижная левая панель (sidebar)
- ✅ Бургер-меню для открытия/закрытия
- ✅ Навигация с иконками:
  - Дашборд
  - График
  - Список сделок
  - ИИ Анализ
  - Настройки
- ✅ Верхняя панель (topbar)
- ✅ Адаптивный дизайн

### 4. Дашборд
**Виджеты (верхний ряд - 4 штуки):**
1. **Общий баланс** - сумма балансов всех демо-счетов
2. **Нереализованный профит** - профит по открытым сделкам (зелёный/красный)
3. **Активные сделки** - количество открытых сделок
4. **Точность ИИ анализа** - процент правильных прогнозов ИИ

**График профита (нижний ряд - на всю ширину):**
- Линейный график истории профита
- Переключатель периодов: 7 / 30 / 180 / 365 дней
- Построен на Chart.js
- Адаптивный, с анимацией

### 5. Демо-счета
**Два счёта для каждого пользователя:**
- 👤 **Пользовательский счёт** — для ручной торговли
- 🤖 **ИИ счёт** — для автоматической торговли ИИ
- Начальный баланс: $10,000 на каждом
- Автоматическое создание при первом входе

### 6. Страницы "В разработке"
- ✅ График
- ✅ ИИ Анализ
- ✅ Настройки

---

## 🔧 API Endpoints

### Авторизация
- `POST /api/auth/login` - вход
- `POST /api/auth/register` - регистрация

### Дашборд
- `GET /api/dashboard/stats?userId=X` - статистика виджетов
- `GET /api/dashboard/profit-chart?userId=X&days=N` - данные графика профита

---

## 📁 Структура проекта

```
crypto-analyzer/
├── kotvukai.db              # SQLite база данных
├── server.js                # Express сервер
├── database.js              # Логика работы с БД
├── package.json             # Зависимости
├── public/
│   ├── index.html           # Страница входа/регистрации
│   ├── styles.css           # Стили авторизации
│   ├── auth.js              # Логика авторизации
│   ├── dashboard.html       # Главная страница приложения
│   ├── dashboard.css        # Стили дашборда
│   └── dashboard.js         # Логика дашборда
└── DATABASE.md              # Документация БД
```

---

## 🎨 Дизайн

- **Цветовая схема:** Тёмная (Emika design system)
- **Шрифты:** Space Grotesk (заголовки), Inter (текст)
- **Цвета виджетов:**
  - Зелёный (#22c55e) - баланс, профит
  - Синий (#3b82f6) - нереализованный профит
  - Жёлтый (#eab308) - активные сделки
  - Фиолетовый (#a855f7) - точность ИИ

---

## 🚀 Запуск

```bash
cd crypto-analyzer
npm install
node server.js
```

Приложение доступно на: `http://localhost:3000`

---

## 📊 Следующие шаги

1. ❌ Раздел "График" - добавление/просмотр криптовалютных графиков
2. ❌ Раздел "Список сделок" - управление сделками
3. ❌ Раздел "ИИ Анализ" - автоматический анализ графиков
4. ❌ Раздел "Настройки" - настройки профиля и системы
5. ❌ Интеграция с криптовалютными API (Binance, CoinGecko)
6. ❌ Реализация ИИ модели для анализа
7. ❌ Desktop версия (Electron)
8. ❌ Windows сборка

---

**Автор:** 祐志 牧谷  
**Название:** KotvukAI  
**Порт:** 3000
